def filter(self, physic=None, type=None):
    """filter the quantity list

    Parameters
    ----------
    physic : list of str, optional
        list of the physic to keep, by default None
        if None, all physic is keep
    type : list of str, optional
        list of the quantity type to keep.
        if None, all type is keep
    """
    return
